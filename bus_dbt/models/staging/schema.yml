version: 2

sources:
  - name: main
    tables:
      - name: trip_punctuality

models:
  - name: stg_trip_punctuality
    columns:
      - name: route_id
        tests:
          - not_null
      - name: delay_sec
        tests:
          - not_null